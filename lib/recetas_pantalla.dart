import 'package:flutter/material.dart';


class RecetasPantalla extends StatelessWidget {
  const RecetasPantalla({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text('Galería de recetas🥩'),
    );
  }
}